name: dinov2
type: image
model_name: dinov2_vits14
output_dim: 384
frozen: true

# DINOv2-ViT-S (smallest variant, loaded via torch.hub).
# CLS token extracted as global image embedding.
# Pooling: mean of patch embeddings (optional; default is CLS).

pooling_method: cls
input_resolution: [128, 128]
normalize_output: true
